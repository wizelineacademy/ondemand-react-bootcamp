const commonTheme = {
  fontSize: '16px',
}

const light = {
  ...commonTheme,
  background: '#ddd'
}

const dark = {
  ...commonTheme,
  background: '#333'
}

const theme = { light, dark }

export default theme