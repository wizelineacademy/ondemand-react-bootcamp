import { Link } from 'react-router-dom';
import styled from 'styled-components';

export const CartSection = styled(Link)`
  background-color: #ff385c;
  color: white;
  display: flex;
  flex-direction: row;
  align-items: center;
  padding: 16px;
  height: 8px;
  cursor: pointer;
  border-radius: 8px;
  margin-right: 8px;
  text-decoration: none;
`;
