import styled from 'styled-components'

const Logo = styled.div`
  img {
    display: block;
    width: 50px;
  }
`
const Title = styled.h1`
  line-height: 1;
  margin: 0;
`

const Styled = {
  Logo,
  Title
}

export default Styled