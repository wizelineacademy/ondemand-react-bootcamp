import styled from 'styled-components'

const ShoppingCart = styled.button`
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 2.5rem;
  padding: 0.5rem;
`

const Styled = { ShoppingCart }

export default Styled